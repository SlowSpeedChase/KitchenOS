# Design Documents — Live Index

The planning layer of the KitchenOS GitOps two-layer system. Tracks active design docs
through their lifecycle. Superseded/shipped legacy plans are frozen in
[archive/INDEX.md](archive/INDEX.md).

**Status flow:** Vision → Ready → In Progress → Done

- **Vision** — idea captured, not yet fleshed out enough to build.
- **Ready** — acceptance criteria + ADHD check + scope check all pass; may start a branch.
- **In Progress** — has an active branch / `BRANCH-STATUS.md`.
- **Done** — merged; move the row here and archive the summary in `docs/completed/`.

Templates: [`templates/DESIGN-DOC-TEMPLATE.md`](../../templates/DESIGN-DOC-TEMPLATE.md) ·
[`templates/BRANCH-STATUS.md`](../../templates/BRANCH-STATUS.md)

---

## Vision

| Date | Doc | Notes |
|---|---|---|
| 2026-08-22 | [capture-queue-retry-cap](capture-queue-retry-cap.md) | `batch_extract` keeps **no attempt counter**, so a permanently dead item is indistinguishable from a transient one and is retried every hour forever — **22 consecutive zero-capture runs** as of 2026-08-22, all one H-E-B URL behind an anti-bot wall that can never succeed without a real browser session. Real failures are buried in the repetition. The fix named by `capture_queue`'s own `fix` field: attempt counter + retry cap + dead-letter path. `classify_error` already splits permanent (`blocked`/`instagram`/`youtube`) from transient (`network`/`ollama`/…), so the cap can be category-aware. **Blocked on three decisions** — what happens to a dead item in Reminders (the queue *is* the Reminders list), where the counter lives, and whether the cap is per-category. |
| 2026-07-09 | [purchase-based-nutrition](purchase-based-nutrition.md) | Use the user's actual purchases to override generic USDA with **branded** label nutrition (more personal + serving-gram data). Blocked: receipts carry no nutrition (only identify the product → still needs OFF/branded lookup) and `purchases`/`inventory` are currently 0 rows. Augments, doesn't replace, the USDA engine. Revisit once receipt ingest is flowing. |

## Ready

| Date | Doc | Notes |
|---|---|---|
| 2026-08-02 | [daily-driver audit](2026-08-02-daily-driver-audit.md) — Phases 2-5 | End-to-end audit of every stage against "make this a daily driver". **Phase 0 (unblock) and Phase 1 (stop lying) are shipped** — see Done. Phase 2 (close the loops) is the next branch: suggest is dead on any board week (`meal_planner.html:5070`), `/prep` blocks for 25s because `llm_gate`'s budget is a *per-attempt* SDK timeout with `max_retries=2` unset, and cooking records nothing (`/api/cook` never writes the `cooks` ledger — there is a pre-existing failing e2e test pinning exactly that). Phases 3-5: fix the nutrition numbers, make the shopping list true, retire the legacy week model. Decisions already taken: assume planned = cooked; retire legacy writes; keep composite meals (which now means making them ledger-native — **shipped 2026-08-03**, see Done); freeze the native app; collapse nav. Note `meal_planner.html:5070` below is stale — that block was deleted; `suggestMeal` is now `:5173`. |

## In Progress

| Date | Doc | Branch | Notes |
|---|---|---|---|
| 2026-07-08 | [macro-meal-planner — design](2026-07-08-macro-meal-planner-design.md) · [plan](2026-07-08-macro-meal-planner-plan.md) | merged to `main` (`a613439`) | **PARTIALLY DELIVERED (2026-07-30).** The single-slot **macro-aware suggester** now ships (`suggest` ranks by waste → macro-gap fit → overlap; new `lib/nutrition_quality.py:macro_eligible` gate; `/api/suggest-meal` returns `macro_context`). This is the narrow, unblocked slice of the parked design — grams coverage was fixed by nutrition-batch-ledger (0.43→0.93). A **review-oriented `scripts/backfill_servings.py`** now estimates missing `servings` from whole-batch calories (calorie-anchor by dish_type), dry-run by default, every write flagged `servings_needs_review` — a widening aid, not the automatic inference that plateaued at ~50%. It also reads a yield the recipe **states** ("Serves 4", "Makes 24 cookies") and writes that as plain fact — unflagged and uncapped — grafted from the parked branch's one unambiguous win; measure nouns ("Makes 2 cups") are excluded. The parked branch `macro-planner-phase-1/servings-backfill` is **retired**: its body/grams/LLM reconciler never cleared its own ≥80%-within-±1 calibration gate. Archive: `docs/completed/2026-07-29-macro-aware-meal-suggestion.md`. Still parked: the full weekly bin-packing planner. |

## Done

| Date | Doc | Completed | Notes |
|---|---|---|---|
| 2026-08-23 | [security-data-integrity repair](../superpowers/specs/2026-08-23-security-data-integrity-design.md) | 2026-08-23 ([PR #75](https://github.com/SlowSpeedChase/KitchenOS/pull/75)) | The privileged Claude web bridge is structurally absent; request-derived Markdown paths are contained; additive and full-set inventory writes are serialized; and valid receipt ledger, purchase, and stock effects commit atomically with source-ID replay protection. Post-commit derived views serialize across processes and cannot turn a committed write into a retryable error. Final verification: 4,108 default tests and 128 E2E passes with no hard failures. Archive: [docs/completed/2026-08-23-security-data-integrity.md](../completed/2026-08-23-security-data-integrity.md). |
| 2026-08-17 | cook-now-staples-demotion (no design doc — spec at [docs/superpowers/specs/2026-08-17-cook-now-staples-demotion-design.md](../superpowers/specs/2026-08-17-cook-now-staples-demotion-design.md)) | 2026-08-17 (`0007223`) | Recipes made **entirely** of pantry staples were permanent squatters on Cook Now — staples count as always-on-hand and never age out, so **Homemade Pasta sat at rank 1** of the live library at 100% coverage, forever. Now a seventh multiplicative factor, parallel to the banked demotion: `recipe_coverage` reports `staple_count` in its existing single pass (5-tuple), and `cook_now` applies `_ALL_STAPLES_WEIGHT = 0.25` when every ingredient is a staple. **Boolean, not a staple-share ramp** — the staple list holds the whole spice rack, so a real curry runs 60–70% staples and a threshold would punish it; the boolean has no tunable to get wrong. One real ingredient escapes entirely (then the recipe only ranks high when that ingredient is on hand, a legitimate claim). Demoted, never hidden; harder than banked (0.25 < 0.5, pinned by test) because banked expires when the freezer empties and all-staples never does. Failure direction is structural: an unparseable ingredient reads as non-staple, so a data gap escapes the demotion rather than triggering it. Payload gains `all_staples`; API.md's stale `/api/cook-now` field list fixed in passing; Kitchen Today's "ready" count deliberately un-gated ("could", not "should"). Measured live: **406 recipes ranked, exactly 2 all-staples — Homemade Pasta 1 → 301, Crispy Spiced Garlic → 387**; API top 5 all real meals. 4018 tests (10 new). Archive: [docs/completed/2026-08-17-cook-now-staples-demotion.md](../completed/2026-08-17-cook-now-staples-demotion.md). |
| 2026-08-03 | plates-ledger-native (no design doc — Phase 5 Decision C of the [daily-driver audit](2026-08-02-daily-driver-audit.md)) | 2026-08-03 | **A plate on a board week now contributes its macros to the day-totals row** — the gap `plates` closed out against and the audit's Decision C, resolved **inverted**: composite meals were *kept and made ledger-native*, not removed. A plate expands to **one ordinary cook per sub-recipe** sharing `cooks.bundle_id`/`bundle_name`, so `day_totals`, the shopping list, the freezer, `cook_history`, `on_track`, `verdict_nudge` and `cook_sweep` needed **no changes at all** — the alternative, a `kind='meal'` row, would have put an "if meal, expand" branch in ~12 consumers. Each member is placed at its `sub_multiplier` share, equal to its scale, which makes `day_totals == meal_nutrition × outer` exact; **verified against all 16 real plates** (Osso Buco Plate lands 1337 kcal / 102 g P). For that identity to hold the two surfaces had to share a gate, so `day_totals` moved from bounds-only to the full `eligible_macros`: **107 of 403 recipes newly excluded** (90 low coverage, 9 unknown servings, 8 both) — but only **one day** across the last three planned weeks (587 → 500 kcal). Coverage fails in the *undercount* direction, so the same commits fixed the surfaces that hid it: `week_view` rendered a fully-excluded day **completely blank**, and `print_week` never carried `excluded` at all. Three live bugs fixed in the seam: the board **spent the pantry twice for every cook** (PATCH consumed server-side, then the client POSTed `/api/cook` again — a prerequisite, since a plate's 🍳 would have tripled it); `_schedule_meal_token` wrote a raw `[[Meal: X]]` that the next regen **silently erased**; and six API routes were ungated. Two traps pinned by breaking them on purpose: the `CREATE INDEX` in `_SCHEMA` that kills **every** `connect()`, and the keystone identity. Biggest lesson: **4003 unit tests passed while the JS grouping stopped drawing every plain cook card** — four e2e tests caught it against a `main` baseline. Still open: 28 ungated routes (now pinned by `KNOWN_UNGATED`) and no browser page sends a bearer header. 4003 tests (from 3927); e2e at parity with main. Archive: [docs/completed/2026-08-03-plates-ledger-native.md](../completed/2026-08-03-plates-ledger-native.md). |
| 2026-08-02 | pantry-spice-rack (no design doc — follow-up to `plates`) | 2026-08-02 (`fac4f72`) | `config/pantry_staples.json` held 26 entries and almost no spices, so **cumin (44 recipes), garlic powder (43), onion powder (33), cinnamon (29), coriander (25), smoked paprika (24) and turmeric (23) counted as real shopping items** — shared shopping between any two recipes using them, and eligible for a list you would never buy from. The other half of the `_is_pantry` fix in `ec0ede0`: that taught the *matcher* that "garlic cloves" is garlic, this fixes the *data*. Kept deliberately separate — garlic powder is a staple by its OWN entry, never by matching `garlic`, because crediting them against each other is the compound-food defect that once dropped fresh garlic from a list. Entries **26 → 50**, ingredient lines credited **1677 → 2132**, spice-driven plate pairings **41 → 0**; 7 new perpetual rows seeded, all verified to carry no `expires`. **"lemon juice" and "lime juice" were proposed and dropped**: `use_it_up._is_staple` matches on token containment *either* direction, so those entries make a plain `Lime` row a staple — and staples never age out, so fresh citrus would have gone invisible to Use It Up. Five existing tests caught it. A new guard states the rule and records the one **pre-existing** offender it found — `onion` covers `Green Onion`, so scallions have never aged out — with a second test that fails once it is fixed, so the allowance cannot outlive the problem. Side effect: `score_overlap` divides by non-staple count, so a smaller denominator raises every score (median non-staple lines/recipe is now 6; 107 of 400 have ≤3), which is why single-ingredient pairings rose 49 → 70 even though the pairings are now real food. Verified live: `garlic powder`/`cumin`/`onion powder` → 0 matches, `lime` → 11, `lemon` → 33. 3883 tests. Archive: [docs/completed/2026-08-02-pantry-spice-rack.md](../completed/2026-08-02-pantry-spice-rack.md). |
| 2026-08-02 | plates (no design doc — brainstormed + approved in session) | 2026-08-02 (`ec0ede0`) | The first **16 curated plates** (main + accompaniments) seeded into `vault/KitchenOS/Meals/`, which had existed and been empty since 2026-06-23. Authored on cooking judgment rather than generated, because measuring `scripts/compose_plates.py` against the live library showed its output was degenerate: **62% of pairings rested on one shared ingredient**, 53% on `garlic cloves` alone, and *Jammy Zucchini* was proposed as the side for **31 of 76 paired mains** — all 31 on garlic and nothing else. Root cause was `_is_pantry` matching a staple exactly or as a **suffix** (`"kosher salt".endswith(" salt")`) while the mirror case stayed open, so `garlic cloves` — the staple plus a form noun — counted as genuine shared shopping. Fixed with an explicit form-noun list (clove/head/bulb/leaf/sprig/stalk) so `garlic powder` and `garlic bread` keep scoring as real foods; verified live after restart: `garlic cloves` → 0 matches, `garlic powder` → 39, `chickpeas, cumin` → 46. The scorer backs `use_it_up`, `cook_now` and `/api/recipes/by-ingredients` too, so all four were inflated. Every plate rolls up macros with **zero excluded components** — only true because of the same day's ingredient-text work. Servings are scaled against each sub-recipe's own stated yield, not a blanket rule (*Easy Hoisin Dipping Sauce* calls its whole batch one serving at 841 kcal, so it sits at 0.15). Plates are **hand-authored artifacts** — the seed script was deliberately not committed so no "regenerate plates" command can exist. Known gap carried forward: a meal card still contributes nothing to a board week's day-totals row (Phase 5 decision C). 3880 tests. Archive: [docs/completed/2026-08-02-plates.md](../completed/2026-08-02-plates.md). |
| 2026-08-02 | ingredient-text-fidelity (no design doc — defect fix) | 2026-08-02 (`aaaac97`) | The 145 EPUB cookbook recipes imported the same day carried **no nutrition at all** — 0 of 145 had calories — so all of them were invisible to day totals, the macro suggester and the new Cook Now ranking. Backfilling exposed **821 ingredient lines that could not be converted to grams**, and the importer was innocent: every fix is in a *reader*, recovering what the recipe already stated. `epub_parser` writes ingredient groups as bolded quantity-less rows and tags them `SUBHEAD_MARKER`, but the marker dies at the file boundary — so **123 rows reached the nutrition engine as ingredients called `**for serving**`**, and seven resolved to *real food* (`**coconut-ginger-lime crisp**` → 15 g / 12 kcal), meaning a section heading counted as a successfully-resolved ingredient. `backfill_nutrition` also carried a **third copy** of the ingredients-section regex CLAUDE.md warns about, which is why Chocolate Peanut Butter Bars reported "skipped — no ingredients" while holding six. Two recoveries followed: **dual-unit package weights** (58 rows) — `gram_equivalent` refused `(15-ounce/425 g)` as "two systems" when it is one quantity glossed twice, now taken **only when the halves agree within 5%**, which is stronger than the lone-figure case it already trusted; and **split amount ranges** (132 rows) — `"1 1/2 to 2 teaspoons dijon mustard"` stored the real unit in the *name* behind a fabricated `whole`. Measured vs main on identical data: gram failures **821 → 599**, stated weights **414 → 472**, mean coverage **0.830 → 0.859**, clearing the trust bar **256 → 280**; 92 recipes improve, 2 drop (both heading-as-food corrections). Live corpus: calories **254 → 400 of 403**, trustworthy **184 → 235**, cookbook imports **0 → 145** with calories and **0 → 46** trustworthy. Mid-branch correction worth keeping: the audit's "unresolved" column is `grams_method`, not food resolution — which surfaced that `coriander seeds` resolves confidently to *Seeds, pumpkin seeds (pepitas)*, a wrong match coverage structurally cannot see (Phase 3 item 17). 3874 tests, ruff unchanged. Archive: [docs/completed/2026-08-02-ingredient-text-fidelity.md](../completed/2026-08-02-ingredient-text-fidelity.md). |
| 2026-08-02 | [daily-driver audit](2026-08-02-daily-driver-audit.md) — Phases 0-1 | 2026-08-02 (`phase-1/honest-system`, `tcc-launcher-binary`, `launchd-path`) | **Phase 0 — capture revived.** Batch extract had produced 18 recipes across 725 runs in 30 days. Root cause was a Full Disk Access grant that could never bind: the LaunchAgent pointed at a bash shim, and `exec` replaces the process image, so macOS evaluated the interpreter — a shell script cannot hold a TCC grant, though the list happily shows it as granted. Agents now support a `binary` launcher kind (an ad-hoc-signed copy of the interpreter inside `.venv/bin/`, so the grant binds without handing Full Disk Access to the shared Homebrew Python). First run after the fix drained the whole queue: 6 of 7 captured, six reminders that had read "Not a URL" for weeks. Also gave every agent a Homebrew-inclusive `PATH` — launchd's bare PATH had been hiding `ffmpeg` (every Instagram extraction silently degraded to caption-only) and `claude` (276 of 277 analysis spawns died while the caller reported success); the generator now fails if a plist omits it. **Phase 1 — the system stopped lying.** A plausibility gate (`lib/nutrition_quality.implausible`) flags 45/252 recipes whose per-serving macros cannot be true; 35 of those had been passing `macro_eligible`, because coverage asks "did every line resolve", which every resolution *error* also answers yes. Measured effect: the macro suggester's top four results for a real 190 g protein target went from the four most broken recipes in the corpus to real food. `day_totals` now excludes and names implausible recipes (2026-08-04: 6018 kcal → 0 kcal, excluded); the dashboard states its denominator instead of reporting an unplanned week as starvation; `/nutrition-review` ranks by violation instead of burying coverage-1.0 catastrophes at the bottom. Three dead controls revived: `/reprocess` (a `cwd` pointing at a path deleted in the macOS rebuild — every Re-extract button in 252 notes), `/refresh` (silently dropping 20 declared frontmatter keys per press), and the failure-analysis agent (now honest and opt-in rather than claiming launches that never happened). New `/system-health` assertions report what is *quietly* not working, with the consequence named. 3433 → 3702 unit tests. |
| 2026-08-01 | recipe-write-safety-followups (no design doc — follow-up fixes) | 2026-08-01 | Closes both follow-ups the previous two branches left open. **(1) Backups.** The review said five writers didn't snapshot before overwriting; a census found four of the five already do, and the real set is three — `cook_history` (syncs into real recipes automatically), `import_crouton` (a filename collision replaces the WHOLE file, `## My Notes` included) and `generate_meal_plan --force`. `backup.write_note()` now makes the safe path the easy one: snapshots first, no-ops on unchanged content so a corpus sync neither spams `.history` nor churns the mtimes sidecar freshness checks read. Generated read-only views are a deliberate exception, not a gap. **(2) Parser agreement.** The schema guard read recipes through `parse_recipe_file` while `enrich_recipes` and Dataview read real YAML — so keys were being validated in a form nothing else used. The gap was **total for block-style lists**: `tags` and `cssclasses` came back as `''` on all 252 files, `equipment` on 8. Flow lists split on a bare `,`, so `["large, well-seasoned cast-iron skillet"]` became two items with half a quote each; `peak_months` read as strings where YAML said ints (hence the `parseInt` in `meal_planner.html`); single-quoted values kept their quote. All fixed, with `_coerce_scalar` shared by plain/flow/block values so they cannot drift again. **Deliberately not switched to `yaml.safe_load`** — it resolves `date_added` to `datetime.date` and every consumer wants ISO strings, so that one divergence is kept, documented, and excluded from the new corpus guard that compares both parsers file by file. That guard found the flow-list bug immediately, which was not in scope. 3637 tests (was 3610), 125 e2e, corpus 0/252, zero new ruff errors. Archive: [docs/completed/2026-08-01-recipe-write-safety-followups.md](../completed/2026-08-01-recipe-write-safety-followups.md). |
| 2026-08-01 | frontmatter-write-safety (no design doc — follow-up fix) | 2026-08-01 | `recipe-schema-normalize` made the corpus guard catch a shredded recipe file; nothing stopped the extractor from **writing** the title that shreds it. Every field a recipe carries is untrusted — `title`, `video_title` and `source_channel` come straight from the YouTube API — and three writers had each hand-rolled `f'"{value}"'`: `templates/recipe_template.py` (6 fields plus `quote_or_null`), `scripts/enrich_recipes.yaml_scalar` (strings *and* list items) and `backfill_nutrition.py`. So `The 9" Skillet` produced frontmatter that did not parse, `---` could end the document mid-value, and a video titled `Chili\nservings: 999` really did inject a key. **`lib/frontmatter.scalar()` is now the one authority** and the local copies delegate to it. `video_title` needed splitting into `video_title_yaml` and `video_title_label`: it is both a YAML scalar and a markdown link label, and JSON-escaping the label would print backslashes at the reader — which is why the bug survived a prior review of that exact line, the value being escaped correctly for one of its two uses. Also widens `require_food_store` to floor `fdc_portions`, since a food list with no gram weights resolves nothing. **Rendering an ordinary recipe is byte-identical to main**, so no re-extraction churn. 3610 tests (was 3579), 125 e2e, corpus still 0/252, zero new ruff errors. Archive: [docs/completed/2026-08-01-frontmatter-write-safety.md](../completed/2026-08-01-frontmatter-write-safety.md). |
| 2026-08-01 | [recipe-schema-normalize](../superpowers/plans/2026-08-01-recipe-schema-normalize.md) | 2026-08-01 | Recipe frontmatter had six producers and no declared schema, so drift accumulated invisibly across 252 files. `lib/recipe_schema.py` now states it (allowlists **measured**, not designed), `scripts/normalize_recipes.py` repairs what it reports, and `tests/e2e/test_recipe_corpus_schema.py` fails if it returns — marked `corpus`, so it runs in the **default** suite rather than only under `-m e2e`. Three inherited premises were wrong and each changed the work: the non-numeric `servings` values **crash nothing** (`week_view`'s bare `float()` sits inside `except Exception: return 4.0`), so the real defect is that `nutrition_engine` reads `"6-8"` as the midpoint **7**, `week_view` as **4.0**, and `macro_eligible` certifies the recipe as trustworthy while they disagree by 75%; `enrich_none` is documented sticky state on **18** files, not debris on 2; and zero of the 13 legacy files needed a carry-across, so that conditional was never written. **Corpus:** 16 files repaired (3 servings ranges → low end, 39 legacy nutrition keys across 13 files, 1 `recipe_url`), `--check` clean at 0/252, second apply a no-op, every body byte-identical. Macros re-derived for the 3 servings-changed recipes (357→446, 221→257, 169→197). **Two incidents worth remembering.** A backfill run from the worktree without `KITCHENOS_DB` silently *created* an empty DB and rewrote three recipes at ~0.3 coverage — one from 357 kcal to **7** — while reporting `Updated: 3, Failed: 0`; recovered from `.history/`. And adversarial review found three reproduced data-corruption paths, none of which could touch today's corpus precisely because each precondition was a docstring rather than a check: `split_frontmatter` split on the *substring* `---` (and `recipe_template` interpolates raw YouTube titles into `video_title`), `remove=` orphaned multi-line values, and the normalizer deleted a legacy key without its canonical twin — the mirror of the `migrate_recipes` rule this same branch had just fixed. All closed. 3579 tests (main 3450), 125 e2e, zero new ruff errors. Archive: [docs/completed/2026-08-01-recipe-schema-normalize.md](../completed/2026-08-01-recipe-schema-normalize.md). |
| 2026-07-30 | [prep-off-the-planner](2026-07-30-prep-off-the-planner.md) | 2026-07-30 | Today's Prep was a **today** object floating on a **week** screen — over the grid, reachable only by opening the planner and expanding a collapsed accordion, which is not where you are when you'd act on it. Now a fifth Kitchen Today card leading to a new **`/prep`** page (tick steps, or push them to a **Prep** Reminders list, separate from Shopping); the panel is deleted from the planner, 108 lines. The card reads the task sidecar **only when fresh** — `extract_tasks` regenerates it with an LLM pass, which cannot happen on a home-page load. Importing `task_extractor` also pulled in the Anthropic SDK at **265 ms**, so that client is now built lazily; `/` came out at **104 ms**, *faster* than its 135 ms baseline. Two build-time corrections: gating Send-to-Reminders on today's steps left a dead end on exactly the days get-ahead work is what you'd queue (now `scope=today|ahead`), and the row being a `<label>` made a click handler fire twice so ticking cancelled itself out — caught by e2e, fixed by listening to `change`. 3065 tests (+15), 8 new e2e. Archive: [docs/completed/2026-07-30-prep-off-the-planner.md](../completed/2026-07-30-prep-off-the-planner.md). |
| 2026-07-30 | [use-it-up-by-item](2026-07-30-use-it-up-by-item.md) | 2026-07-30 | Use It Up rendered **ten lime recipes and nothing for the ham expiring that day**. Three stacked defects: the ranking's operative tiebreak was *shortest recipe name* (with one matchable item everything ties on `uses_count` and `urgency`); `sliced ham off the bone` parsed its head noun as **`bone`** and carried `{bone, off}` in its token set, so it could not match `deli ham` by head *or* by containment — the matcher was looking for a food called bone; and a flat list can only report "nothing uses this" by omission. Now item-first: each at-risk item carries its own recipes, ranked by **`recipe_coverage`** — extracted into `use_it_up` and shared with `cook_now`, which already imported this module's matching machinery, so there is one implementation rather than two that can drift (cf. the `unit_compatibility` invariant). `of` is deliberately *not* stripped from names: it would turn `Cream of tartar` into `cream` and match every cream in the library. Live result: ham **0 → 3 recipes**, top one at **100% coverage**. The panel dock is also capped so opening one panel can no longer push the other off screen. 3050 tests (14 new), 90 e2e. Archive: [docs/completed/2026-07-30-use-it-up-by-item.md](../completed/2026-07-30-use-it-up-by-item.md). |
| 2026-07-30 | [recipe-short-titles](2026-07-30-recipe-short-titles.md) | 2026-07-30 | The extraction prompt never said `recipe_name` should be a *dish* name, so the models returned video titles — **70 of 252 names are over 32 chars** against a ~104px planner card. Fixes the prompt going forward and adds an optional `short_title` frontmatter field for what already exists, surfaced as `display_name`. **The filename stays the identity** (`cooks.recipe`, meal plans and the task-ID hash are all keyed on it, so renaming would orphan planned cooks and reset task checkboxes). `validate_short_title` requires the candidate to be a *subsequence* of the original — shortening deletes words, so it can neither invent nor reorder them — with non-Latin names exempt because they must be translated. Backfilled 70: every rendered title is now ≤32 chars, zero collisions. Measured Ollama 8/16 vs Claude 13/16 on the same recipes with the same validator. **Merged `0e9c387` (PR #50).** Archive: [docs/completed/2026-07-30-planner-shelf-and-short-titles.md](../completed/2026-07-30-planner-shelf-and-short-titles.md). |
| 2026-07-30 | [planner-shelf-and-tap-to-assign](2026-07-30-planner-shelf-and-tap-to-assign.md) | 2026-07-30 | Measured on a real browser at iPad size: **526 of 607** visible controls sit below the 44 pt floor, the recipe library is hidden behind a FAB in portrait while ~700 px sits empty, and 26 of 28 grid cells advertise "Drop recipe" — the only verb the screen names. Rotates the sidebar into a persistent bottom **shelf** in portrait (landscape's docked rail already works, untouched), compresses empty cells so the whole week fits above it, and adds **tap-to-assign** (arm a recipe, tap a slot) alongside drag per [WCAG 2.2 SC 2.5.7](https://www.w3.org/WAI/WCAG22/Understanding/dragging-movements.html). Deferred to sibling docs: Today's Prep relocation, and the Use It Up rework (it ranks by *shortest recipe name* today — `lib/use_it_up.py:252` — so it shows 10 lime recipes and none for the ham expiring today, while the wanted pantry-coverage ranking already exists unused at `meal_suggester.score_overlap`). **Merged `a10e5c8` (PR #47).** Archive: [docs/completed/2026-07-30-planner-shelf-and-short-titles.md](../completed/2026-07-30-planner-shelf-and-short-titles.md). |
| 2026-07-30 | [kitchen-today-home — design](../superpowers/specs/2026-07-30-kitchen-today-home-design.md) | 2026-07-30 | Replaced the Obsidian canvas homepage — unusable on a phone for **structural** reasons (both canvases embed `Dashboard.md`'s five Dataview blocks, each scanning all 252 recipes; canvas nodes are absolute pixels, so a 1560px node needs 4× zoom-out at 390pt, and canvas has no responsive reflow). The framing came from asking: the real failure was **recall, not navigation** ("I forget the features exist"), which rules out a better menu — so `/` became four live cards, each a fact that doubles as a workflow entry point. `lib/kitchen_today.py` parses the library **once** and injects it into `cook_now`/`use_it_up` (which each re-parse when called bare); every card degrades to a plain link so none can 500 the page. **135 ms.** New `/recent` ordered by file **birth** time — mtime reshuffles on every nutrition backfill. Diagnosed en route: shopping-list generation was **never broken** (W31 preview succeeded all along) — its only trigger was an Obsidian `kitchenos://` button whose macOS helper app no longer exists, and which never worked from a phone regardless; that is why no list existed between W27 and W31. `/current/*` now render HTML instead of 302'ing to `obsidian://`, with working HTTP buttons for **both** dead actions (verified live: 24 items generated, 24 sent to Reminders). Two defects caught mid-branch: button dispatch inferred the action from the *page's* week, collapsing a note's three buttons into one; and `lib/reminders.py` interpolated LLM-extracted ingredient text into AppleScript with escaping that missed backslashes — `a" & (do shell script "…") & "b` was a **working command-execution payload**, now passed as `argv`, and batched from one subprocess per item into one call. 2881 → 2897 tests (54 new), e2e unchanged, zero new ruff errors, all pages verified at 390pt in both themes. Archive: `docs/completed/2026-07-30-kitchen-today-home.md`. |
| 2026-07-26 | [inventory-location-visibility — design](../superpowers/specs/2026-07-26-inventory-location-visibility-design.md) · [plan](../superpowers/plans/2026-07-26-inventory-location-visibility.md) | 2026-07-29 | Show each item's storage location on `/review`, group the list by location, and record on every row *how* its location was decided so a machine guess stays distinguishable from a placement you confirmed. **MERGED + LIVE 2026-07-29.** New `place_item()` router returns `(location, source)`; the tier is stored as `inventory.location_source` and backfilled once — self-healing, keyed on the NULLs rather than the ALTER, and proven by reproducing the crash. Live split after migration: **199 category / 11 item / 5 manual / 7 default** of 222, 0 NULLs. A hand-correction stamps the row `manual` **and** teaches `config/storage_locations.json`, so the same wrong guess stops recurring. Scope added at the rebase: the same subline now surfaces `last_used`/`use_count`, which `consume-on-cook` shipped with no reader. Two test-isolation layers were needed after the real config was observed being rewritten — in-process *and* the out-of-process e2e server. Verified at merge: 2827 unit (main 2798), 36 e2e + 3 xfail + 1 xpass, ruff identical to main. **Carried forward, and the reason this isn't fully honest yet: the iOS app stamps every added row `manual` from a Picker default** (`InventoryView.swift`), so app-added rows record an untouched form default as a confirmed placement — client-side Swift fix, should be next. Archive: `docs/completed/2026-07-29-inventory-location-visibility.md`. |
| 2026-07-26 | [consume-on-cook — design](../superpowers/specs/2026-07-26-consume-on-cook-design.md) · [plan](../superpowers/plans/2026-07-26-consume-on-cook.md) | 2026-07-27 | Marking a recipe cooked did essentially nothing: 234 of 236 recipes changed no inventory, 2 of 2,634 ingredient lines subtracted, and the UI showed a green toast regardless. Fixed the three coupled defects (one `unit_compatibility` predicate both `split_against_pantry` and `apply_decisions` delegate to; `find_match` adopting the shared head-noun matcher; one `renderCookToast` reporting all four outcomes) on top of the governing insight that **inventory holds containers, not quantities**. Review then caught the gate under-firing in ways that *deleted live data*: weight packages aren't qty 1.0, so the real `5 oz` pumpkin row was destroyed by `250 g pumpkin puree` (222 → 221 rows); rows summed across locations hid the parts from the gate while `save_pantry` collapsed one away with its stamps; and a cumulative-subtraction gap let two passing lines jointly empty a row. The invariant is now the absolute one — **a cook may reduce a row but never delete one** — in four cases. Review also found the identical read-only-`consumed` defect in `mcp_server.cook_recipe`, which the plan never mentioned, and a `use_count` double-increment. Final census: **19 decrements, 453 use-stamps across 90 rows, 198 recipes reporting, zero false decrements** (spec predicted 18 / 455 / 90 / ~199). `/api/cook` is now token-gated. 2691 → 2715 tests, 25 → 28 e2e. See [docs/completed/2026-07-27-consume-on-cook.md](../completed/2026-07-27-consume-on-cook.md). |
| 2026-07-25 | [cook-now-meal-type-filter — design](../superpowers/specs/2026-07-25-cook-now-meal-type-filter-design.md) · [plan](../superpowers/plans/2026-07-25-cook-now-meal-type-filter.md) | 2026-07-25 | Filter Cook Now by meal type. Half data repair, half feature: `dish_type` had drifted (12 one-off values, and `normalizer.py` mapped `"biscuit" → "dessert"`, so savory biscuits were filed as desserts), so a filter built on it would have been wrong in both directions. `VALID_DISH_TYPES` is now derived from `DISH_TYPE_MAP`'s targets, the biscuit rule is deleted so the repair can't rot, and `scripts/reclassify_dish_type.py` (Claude Batches, enum-constrained, dry-run by default) reclassified **63 of 239 recipes, 0 unresolved**. On top of that: `/cook-now` with six chip groups over the 13 values, filtering client-side with Desserts deselected by default. Review caught a non-string `dish_type` that would have 500'd the whole endpoint and a `GROUPS` array that could silently drift from the Python taxonomy. 1476 → 1504 tests, 22 → 25 e2e. See [docs/completed/2026-07-25-cook-now-meal-type-filter.md](../completed/2026-07-25-cook-now-meal-type-filter.md). |
| 2026-07-25 | [bulk-inventory-editing — design](../superpowers/specs/2026-07-25-bulk-inventory-editing-design.md) · [plan](../superpowers/plans/2026-07-25-bulk-inventory-editing.md) | 2026-07-25 | Mass select + edit on `/review` (checkboxes, Select All, sticky action bar) backed by one `POST /api/inventory/bulk` doing a single read-modify-write instead of N, addressed by the real `(name, unit, location)` key rather than the ambiguous `(name, location)`. Mid-branch the `+3d`/`+7d` buttons were also made cumulative — they set `today + N` outright, so a second tap was a no-op and `+3d` after `+7d` moved the date backward — and the list gained an Expiry/Added sort backed by `purchased`. The plan's manual phone script was automated instead: `tests/e2e/` is a Playwright harness the plan said didn't exist. 1426 → 1476 tests. See [docs/completed/2026-07-25-bulk-inventory-editing.md](../completed/2026-07-25-bulk-inventory-editing.md). |
| 2026-07-25 | inventory-truth-fixes (no design doc — bugfix) | 2026-07-25 | `Inventory.md` rendered empty while the DB held 219 items, and Cook Now ranked only desserts. Three fixes: `write_inventory()` now renders from the committed DB instead of the caller's list (it and `cook_now` were two sources in one function); token matching requires containment to reach the head noun of a *clean* name, killing "eggs" ↔ *Lo mein egg noodles* and "avocado" ↔ *Avocado oil* while leaving noisy ingredient text on plain containment; pantry staples are materialized as perpetual `source: staple` rows instead of an invisible assumption. 60 recipes de-inflated, 54 false-positive ingredients gone, 0 regressions. 1404 → 1426 tests. Root cause of the empty write is still unknown. See [docs/completed/2026-07-25-inventory-truth-fixes.md](../completed/2026-07-25-inventory-truth-fixes.md). |
| 2026-07-25 | [web-home-page](../superpowers/specs/2026-07-25-web-home-page-design.md) · [plan](../superpowers/plans/2026-07-25-web-home-page.md) | 2026-07-25 | A web home page at `/` rendered from the `SECTIONS` registry, plus a `ko-home-link` in `_CLAUDE_BAR_TEMPLATE` so every page links back with no per-template edits. `SECTIONS` now feeds three consumers; `HOME` added as the registry root (outside `SECTIONS`, else the page lists itself), which required fixing `desired_bookmarks()` — it built from `SECTIONS` alone, so the home bookmark would never have reached Safari. 1386 → 1405 tests. See [docs/completed/2026-07-25-web-home-page.md](../completed/2026-07-25-web-home-page.md). |
| 2026-07-12 | [photo-receipt-ingest](../superpowers/specs/2026-07-12-photo-receipt-ingest-design.md) | 2026-07-12 | Photograph an HEB receipt in the Claude iOS app → paste the schema JSON at `/receipt-paste` → full trip/inventory pipeline. Shared `lib/receipt_ingest.py:ingest_parsed` (extracted from `process_email`, email path unchanged), content-hash dedup, `POST /api/receipt/paste`. Zero server-side LLM. See [docs/completed/2026-07-12-photo-receipt-ingest.md](../completed/2026-07-12-photo-receipt-ingest.md). |
| 2026-07-10 | [nutrition-batch-ledger](nutrition-batch-ledger.md) | 2026-07-10 | **Phase 2 core shipped** (Fable batch/ledger reframe). Offline calorie coverage **0.434 → 0.929**, item 0.503 → 0.972, fully-covered 11% → 75%, grams-failed 388 → 12; engine now fully offline (no runtime USDA). A (Atwater) + B (bulk FDC local store, 13.7k foods) + C (LLM portion ledger, band-validated). Component D + review-queue-note + golden-set + vault re-backfill are follow-ups. See [docs/completed/2026-07-10-nutrition-batch-ledger.md](../completed/2026-07-10-nutrition-batch-ledger.md). |
| 2026-07-09 | [nutrition-portion-resolution](nutrition-portion-resolution.md) | 2026-07-09 | **Phase 1 shipped.** 5 fixes (FDC volume portions, 429 backoff, energy nutrient-ID, offline meter, caloric-sanity guard). Dominant bug was food-data quality, not portions: energy under Atwater IDs → 0-kcal foods. Verified +31–366% on 5 recipes. Follow-ups (vault re-backfill, food-match depth) tracked in the doc. See [docs/completed/2026-07-09-portion-resolution.md](../completed/2026-07-09-portion-resolution.md). |
| 2026-07-08 | [ingredient-data-cleaning](ingredient-data-cleaning.md) | 2026-07-09 | Phase A1 shipped: nutrition-engine grams coverage **0.563 → 0.647** (+8.4 pts, 30-recipe sample) via unit/piece-weight/density/accent table gaps. Full-vault `backfill_nutrition.py --force` applied (228 updated, 0 failed). Phase A2 (leaked amounts) + Phase B deferred, still tracked in the design doc. See [docs/completed/2026-07-09-ingredient-grams-coverage.md](../completed/2026-07-09-ingredient-grams-coverage.md). |
| — | — | — | See [archive/INDEX.md](archive/INDEX.md) for pre-convention legacy plans. |
